# COST-7689 Gap Analysis: Implement RBAC Service

**Jira:** [COST-7689](https://redhat.atlassian.net/browse/COST-7689)
**Audited:** 2026-08-10 · **Closed (docs):** 2026-08-17
**Purpose:** Handoff audit vs ticket acceptance criteria. Does not update `docs/tasks.md` or other trackers.
**Beta scope:** ROS and Kruize are **out of beta** ([BETA-PRIORITY.md](BETA-PRIORITY.md)); optional install → [COST-8054](../jira/COST-8054.md). RBAC API readiness is a Cost beta concern; unrelated to ROS or Kruize.

## Verdict

**COST-7689 is done** for the ticket-scoped deliverables that remain here.
RBAC workloads go beyond the ticket’s worker-only wording (API Deployment +
Service + Celery worker, Stage 4 before Koku, DB/cache env wiring, Keycloak
sync). **G2 (RBAC readiness status) is closed**: `RBACReady` gates on the RBAC
API Deployment before `Available`. **G1 (profile-based resource sizing)** moved
to [COST-8095](https://redhat.atlassian.net/browse/COST-8095) — not required to
close this ticket.

## Sources

- [Jira COST-7689](https://redhat.atlassian.net/browse/COST-7689)
- [docs/jira/COST-7689.md](../jira/COST-7689.md)
- [`internal/resources/rbac.go`](../../internal/resources/rbac.go) — API/worker builders + `rbacEnv`
- [`internal/resources/rbac_test.go`](../../internal/resources/rbac_test.go)
- [`internal/controller/costmanagementserviceconfig_controller.go`](../../internal/controller/costmanagementserviceconfig_controller.go) — Stage 4 `reconcileCoreServices`
- [`api/v1alpha1/costmanagementserviceconfig_types.go`](../../api/v1alpha1/costmanagementserviceconfig_types.go) — `RBACConfig` / `RBACComponentSpec` / condition constants
- [`internal/resources/names.go`](../../internal/resources/names.go) — `NameRBACAPI` / `NameRBACWorker` / `DatabaseHost` / `CacheHost`
- [docs/code-review-fixmes.md](../code-review-fixmes.md) #1 (readiness gates); D18 (`REDIS_PORT`) is fixed

Out of scope per ticket: RBAC schema migration and data seeding (story 9 / COST-7685).

---

## Ticket acceptance criteria

| Deliverable | Status |
|-------------|--------|
| RBAC worker Deployment with env wiring to external DB and cache | Done (superseded: also deploys RBAC API + Service) |
| Profile-based resource sizing | Moved to [COST-8095](https://redhat.atlassian.net/browse/COST-8095) (post-beta) |
| Status reporting for RBAC component readiness | **Done** — `RBACReady` + gate before `Available` |

---

## Done (meets or supersedes ticket intent)

Implemented primarily in [`internal/resources/rbac.go`](../../internal/resources/rbac.go), applied in Stage 4 (`reconcileCoreServices`) of [`costmanagementserviceconfig_controller.go`](../../internal/controller/costmanagementserviceconfig_controller.go) (~L416–426):

### Workloads

- `RBACWorkerDeployment` — Celery worker (`celery -A rbac.celery worker --pool=solo`)
- `RBACAPIDeployment` + `RBACAPIService` — Gunicorn on port 8080 (not in ticket body; required so Koku/Envoy can call RBAC)
- Applied **before** Koku API so permission checks have a target
- Init containers: `waitForRBACDB` + `WaitForValkeyInitContainer`
- API liveness/readiness probes on `/api/rbac/v1/status/`
- Names: `<cr>-rbac-api`, `<cr>-rbac-worker` ([`names.go`](../../internal/resources/names.go))

### External DB + cache env wiring (`rbacEnv`)

| Concern | Wiring |
|---------|--------|
| DB host/port | `DATABASE_HOST` / `DATABASE_PORT` via `DatabaseHost` + `spec.database.port` (default 5432) |
| DB name | `DATABASE_NAME=costonprem_rbac` (`rbacDBName`) |
| DB credentials | `DATABASE_USER` / `DATABASE_PASSWORD` from `<cr>-db-credentials` keys `rbac-user` / `rbac-password` |
| SSL mode | `PGSSLMODE` from `spec.database.sslMode` (default `disable`) |
| Cache host | `REDIS_HOST` via `CacheHost` (bundled Valkey name or external host) |
| Cache auth | Optional `REDIS_USERNAME` / `REDIS_PASSWORD` when `cache.auth.enabled` + `secretName` |
| Cache TLS | `REDIS_SSL` + CA mount when `cache.tls.enabled` |
| Django secret | `DJANGO_SECRET_KEY` from operator Django secret |

Also sets on-prem RBAC flags mirroring the Helm chart helpers (`BYPASS_BOP_VERIFICATION`, `KAFKA_ENABLED=false`, V2 bootstrap UUID placeholders, `API_PATH_PREFIX=/api/rbac`).

### CR surface

[`RBACConfig`](../../api/v1alpha1/costmanagementserviceconfig_types.go): shared `image`, per-component `api` / `worker` with `replicas` (CRD default 1) and `resources` overrides, plus bootstrap/keycloak sync fields used by migration tickets.

### Tests

- [`rbac_test.go`](../../internal/resources/rbac_test.go) asserts `API_PATH_PREFIX=/api/rbac`
- [`core_services_test.go`](../../internal/controller/core_services_test.go) — `RBACReady` / `WaitingForRBAC` gate (API only; worker does not block `Available`)
- Controllers/other packages exercise RBAC naming in Envoy backend tests and DB secret key validation (`rbac-user` / `rbac-password`)

---

## Intentional deviations (do not treat as bugs)

| JIRA | Implementation | Rationale |
|------|----------------|-----------|
| “Deliver RBAC worker Deployment” | Also deploys RBAC API Deployment + ClusterIP Service | Koku/Envoy call the API; worker alone is insufficient for on-prem |
| Worker listed under workers/platform stories elsewhere | RBAC applied in Stage 4 before Koku | Comment in controller: must be up before Koku serves requests |
| Linear phase names in older backlog | Conditions + simplified `Phase` | Same operator-wide design as COST-7678/7680 |
| Migration/seed in “story 9” | Out of this ticket; implemented under COST-7685 | Explicit out of scope |

---

## Remaining gaps (ticket-scoped)

### G1. Profile-based resource sizing — moved to [COST-8095](https://redhat.atlassian.net/browse/COST-8095)

RBAC API/worker still pass through `spec.rbac.*.resources` only. Shared
`spec.profile` maps (including RBAC rows) are owned by COST-8095, not this
ticket. Same UI-only wiring as today ([`uiProfileResources`](../../internal/resources/ui.go)).

### G2. Status reporting for RBAC readiness — closed

`ConditionRBACReady` in [`costmanagementserviceconfig_types.go`](../../api/v1alpha1/costmanagementserviceconfig_types.go). Stage 4 applies RBAC + Koku in parallel, then `isDeploymentReady` on `NameRBACAPI` (not the worker). Not ready → `RBACReady=False` / `Available=False` reason `WaitingForRBAC`. Ready → `RBACReady=True` (`RBACAvailable`), then the existing Koku API gate. The worker is reported separately as `RBACWorkerReady` and does not gate `Available`. Tests in [`core_services_test.go`](../../internal/controller/core_services_test.go).

---

## Related gaps (not in Jira body; same surface)

### R1. `REDIS_PORT` hardcoded to `6379` — closed

[`rbacEnv`](../../internal/resources/rbac.go) uses `cachePortStr(cfg)` (same as Koku / wait-for-Valkey).

### R2. Thin unit coverage for RBAC builders

Only `API_PATH_PREFIX` is asserted. No tests for DB/cache secret key refs, TLS/auth env branches, replica defaults, or that worker/API Deployments are produced with expected names/commands.

### R3. No readiness wait before Koku in Stage 4 — accepted

Koku is still applied in the same pass as RBAC (faster cold start). `Available` waits on the RBAC API (G2). A Koku wait-for-RBAC init container is out of scope.

